export class UserModel
{
    /**
     * get list of users
     * @param {Object} request
     * @returns
     */
    getData = async (request: object) => {
        return request;
    };


    /**
     * get detail of users
     * @param {Number|String} request.id
     * @returns
     */
    detailData = async (request: object) => {
        return request;
    }


    /**
     * insert new user
     * @param {Object} request
     * @returns
     */
    insertData = async (request: object) => {
        return request;
    }


    /**
     * Update User
     * @param {Object} request
     * @returns
     */
    updateData = async (request: object) => {
        return request;
    }


    /**
     * Delete User
     * @param {Number|String} request.id
     * @returns
     */
    deleteData = async (request: object) => {
        return request;
    }
}